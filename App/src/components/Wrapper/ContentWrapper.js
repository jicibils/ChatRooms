// Content loader, renders the content depending the routes
import React from 'react';
import DashboardRoutes from 'routes/dashboardRoutes';

const ContentWrapper = () => <DashboardRoutes />;

export default ContentWrapper;
